# Setup SQL moved

All Supabase setup SQL lives in the private docs repo: `mapstructor_docs/sql/` (one canonical folder, every file re-runnable).
It was consolidated 2026-07-15 after duplicate copies caused days of failed pmtiles uploads.
