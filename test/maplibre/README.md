# test/maplibre — MapLibre migration test area

Everything MapLibre-related that isn't yet part of the engine lives here, one subfolder per
experiment (todo → FREE-STACK MIGRATION section for the full picture).

- `timeline/` — the renderer A/B slider twins (timeline-crispness comparisons only).
  Future experiments (pmtiles-only pages, basemap swaps, engine spikes) get their own folders.
